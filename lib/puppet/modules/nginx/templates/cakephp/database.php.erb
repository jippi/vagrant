<?php
// Managed by Puppet - will be overwritten
// every time you provision, please don't change

class DATABASE_CONFIG {

  var $default = array(
    'datasource'    => 'Database/Mysql',
    'persistent'    => true,
    'host'          => '<%= database_host -%>',
    'login'         => '<%= database_username -%>',
    'password'      => '<%= database_password -%>',
    'database'      => '<%= database_name -%>',
    'prefix'        => '',
    'encoding'      => 'utf8',
  );

  var $test = array(
    'datasource'    => 'Database/Mysql',
    'persistent'    => true,
    'host'          => '<%= database_host -%>',
    'login'         => '<%= database_username -%>',
    'password'      => '<%= database_password -%>',
    'database'      => '<%= database_name -%>_test',
    'prefix'        => '',
    'encoding'      => 'utf8',
  );

  var $test_seed = array(
    'datasource'    => 'Database/Mysql',
    'persistent'    => true,
    'host'          => '<%= database_host -%>',
    'login'         => '<%= database_username -%>',
    'password'      => '<%= database_password -%>',
    'database'      => '<%= database_name -%>_seed',
    'prefix'        => '',
    'encoding'      => 'utf8',
  );

  public function __construct() {
    $connections = Configure::read('DATABASE_CONFIG');
    if (empty($connections)) {
      return;
    }

    foreach ($connections as $name => $config) {
      $this->{$name} = $config;
    }
  }

}
